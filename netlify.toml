[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA React Router : /login, rechargement, liens directs
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

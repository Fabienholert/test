# --- Frontend (racine du repo, build Vite / Netlify) ---
# URL de l’API (obligatoire en prod sur Netlify si vous ne voulez pas le fallback du code)
VITE_API_URL=https://audit-garantie-vw-api.onrender.com/api

# Dev local : proxy Vite sur /api ou backend sur 5001
# VITE_API_URL=http://localhost:5001/api

# --- Backend (dossier backend/, ou variables du service Render) ---
# Copier backend/.env.example vers backend/.env pour le local

FRONTEND_URL=https://testgarantie.netlify.app
MONGODB_URI=
JWT_SECRET=
GMAIL_USER=
GMAIL_PASSWORD=

# Ancien nom (CRA) — non utilisé par Vite
# REACT_APP_API_URL=http://localhost:5000/api

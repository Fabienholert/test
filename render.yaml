services:
  - type: web
    name: audit-garantie-vw
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: |
      npm install && npm run build
      cp -r backend/ dist/
      cd dist/backend && npm install
    startCommand: "cd dist && node backend/server.js"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production

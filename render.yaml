services:
  - type: web
    name: audit-garantie-vw
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: |
      npm install && npm run build
      cd backend && npm install
    startCommand: "node backend/server.js"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production

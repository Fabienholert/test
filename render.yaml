services:
  - type: web
    name: audit-garantie-vw
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: "npm install && cd client && npm install && npm run build"
    startCommand: "node backend/server.js"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        fromDatabase:
          name: audit-garantie-vw-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
